/* /src/frontend/pages/Settings.css */
.settings-page {
  max-width: 800px;
  margin: 0 auto;
  padding: 20px;
}

.settings-page h1 {
  color: var(--primary-color);
  margin-bottom: 20px;
}

.settings-section {
  background-color: white;
  border-radius: var(--border-radius);
  padding: 20px;
  margin-bottom: 20px;
  box-shadow: var(--box-shadow);
}

.settings-section h2 {
  color: var(--secondary-color);
  margin-bottom: 15px;
  font-size: 1.2rem;
}

.settings-options {
  margin-top: 20px;
  display: flex;
  flex-direction: column;
  gap: 15px;
}