/* /src/frontend/components/common/ErrorMessage.css */
.error-message {
  background-color: rgba(231, 76, 60, 0.1);
  border-left: 4px solid var(--error-color);
  color: var(--error-color);
  padding: 10px 15px;
  margin: 10px 0;
  border-radius: 0 var(--border-radius) var(--border-radius) 0;
  font-size: 14px;
}